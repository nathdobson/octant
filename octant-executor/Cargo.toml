[package]
name = "octant-executor"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = {workspace=true}
tokio = {workspace=true,features = ["sync"]}
futures = {workspace=true}
parking_lot = {workspace=true}
